package ex_04_operators;

public class Lab0024_reational_operators {
    static void main() {
        // releationship operator always gives you Answer in boolean.
        // < = less than
        // > = greater than
        // (<=) = less than is equal to
        // (=>) = greater than is equal to
        // (==) = compare to
        // !=  = not equal to

        }
}
